# Database Configuration
# Override these values as needed for your environment

# Use 'host.docker.internal' to connect to services running on your host machine
# Use 'localhost' if database is on host and you're using --network="host"
# Use container name if database is in another Docker container
DB_HOST=host.docker.internal
DB_USER=sa
DB_PASSWORD=Gogogo123!
DB_NAME=TradingBE
